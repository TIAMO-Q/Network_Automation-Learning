# 09 异常处理
### 1.何为异常
异常指的就是程序发生错误的信号，程序一旦出错就会抛出异常。随着异常的出现，程序也随之终止。

```python
Traceback (most recent call last):
  File "E:\devops\Phase 2\project\自动化编程\5-20\异常处理.py", line 1, in <module>
    name
NameError: name 'name' is not defined
```

关于报错的信息，一般分为三个部分：

1.Traceback (most recent call last): 异常的定位，显示错误发生在哪个文件下的哪一行

2.name   显示是什么类型的报错

3.NameError: name 'name' is not defined ：显示错误的具体信息

### 2.为何需要进行异常处理
1. 增强程序的可靠性，也就是说即便程序运行过程中出错了，也不要终止程序
2. 异常其实是一种资源，可以反映出很多信息，也能够让我们更加的了解到自己错误的地方，因此要捕捉异常并做出相应处理，然后也将出错信息记录到日志内



### 3.怎样处理异常
#### 3.1 异常从何而来
+ 语法错误

只要出现了语法错误，就必须严格按照语法规定来进行修改

```python
# try:
#     name
# except Exception:
#     print("语法错误")
#
# print(name)
```

+ 逻辑错误
1. 可以预知的错误

```python
ip = input("请输入需要查询的ip：")
ip_list = ip.split(".")
print(ip_list)

if ip_list.__len__() != 4:
    print("IP地址格式输入错误，正确的格式为x.x.x.x")

else:
    for i in ip_list:
        if not i.isdecimal():
            print("IP地址的每一段必须为整数")
            break
        if int(i) < 0 or int(i) > 255:
            print("IP地址的每一段的范围必须是0-255的整数")
            break
        else:
            res = bin(int(ip_list[0])).replace("0b", "").zfill(8)
            if res.startswith("0"):
                print("该地址为A类地址")
            elif res.startswith("10"):
                print("该地址为B类地址")
            elif res.startswith("110"):
                print("该地址为C类地址")
            elif res.startswith("1110"):
                print("该地址为D类地址")
            elif res.startswith("1111"):
                print("该地址为E类地址")
```

2. 无法预知的错误

比如正在运行的服务器，已经正在连接的网络，不知道什么时候会挂掉。如果一旦挂掉，此后进行远程连接时，就会出现报错，此时由于要用到很多模块，因此报错的内容可能不太明确，此时要用到异常处理：考虑如果服务器挂掉要怎么办以及如果网络断掉要怎么处理..

#### 3.2 异常处理基本语法结构
```python
try:  # 尝试去执行的代码(可能会出现异常的代码)
    # Tel = telnetlib.Telnet("192.168.1.1")
    name
except Exception as e:  # 如果try下执行的代码出现异常，那么except会捕捉到，此时会执行except下的代码块,同时会将错误的内容赋值给e
    print(e)
else:  # 如果没有捕获到异常，代码正常执行的情况下，会执行else下的代码块
    print("success")
finally:  # 无论是否捕获到异常，都会执行finally下面的代码块
    print("hello")
```

```python
l1 = [1, 2, 3, 4]
l1_iter = iter(l1)
while True:
    try:
        print(next(l1_iter))
    except StopIteration:
        print("迭代完毕")
        break
```



### 4.自定义异常&主动抛出异常
自定义异常是为了让报错的信息更加的直观，以及更加容易被人理解

+ 如何自定义异常

```python
class LoginError(Exception):
    def __init__(self, ip):
        self.ip = ip

    def __str__(self):
        return f"{self.ip}登录失败"

    # 在python当中，所有以__开头 同时以__结尾的方法，都叫做魔法方法
    # 魔法方法的含义是在满足某些特定的情况下会自动的触发调用
    # 而__str__方法就是在print一个对象时，会自动的触发


a = LoginError(ip="1.1.1.1")
print(a)
```

+ 如何主动抛出异常，并且显示自定义的异常内容

```python
class LoginError(Exception):
    def __init__(self, ip):
        self.ip = ip

    def __str__(self):
        return f"{self.ip}登录失败"

ip = "192.168.1.1"
try:
    try:
        Telnet(ip)
    except Exception:
        raise LoginError(ip)
except LoginError as e:
    print(e)
```













·
