# 04 数据类型
### 1 list 列表
> 列表（list），是一个有序且可变的容器，在里面可以存放多个不同类型的元素
>

#### 1.1 定义
```python
v1 = [] #定义一个空列表
v1 = [1, 2, 3, "a", "b", []]
```

#### 1.2 独有功能
##### 1.2.1 append 追加
在列表的末尾添加一个元素

```python
v1 = [1, 2, 3, "a", "b", []]
v1.append("c")

print(v1)
```

```python
v1 = [1, "b", []]
print(id(v1))
print(v1)
v1.append("c")
print(id(v1))
print(v1)
```

##### 1.2.2 extend 批量追加
```python
v1 = [1, 2, 3, "a", "b", []]
v2 = ["d", "e", "f"]
for i in v2:
    v1.append(i)
print(v1)

v1 = [1, 2, 3, "a", "b", []]
v2 = ["d", "e", "f"]
v1.extend(v2)
print(v1)
```

##### 1.2.3 insert 插入
在列表的某个位置插入一个元素

```python
v1 = [1, 5, 3, "a", "b", []]
v1.insert(2, [4])
print(v1)
```

##### 1.2.4 remove 删除
```python
在for循环的情况下，使用remove删除，可能会出现意外的错误
v1 = [1, 1, 1, 1, 2, 3, 1, 1]
for i in v1:
    if i == 1:
        v1.remove(i)
print(v1)
# 第一次循环0号索引 [1, 1, 1, 2, 3]
# 第二次循环1号索引[1, 1, 2, 3]
# 第三次循环2号索引[1, 1, 2, 3]

# 第四次循环3号索引[1, 1, 2, 3]

解决办法：倒着删
v1 = [1, 2, 3, 1, 1]
for i in v1[::-1]:
    if i == 1:
        v1.remove(i)
print(v1)

v1 = [1, 1, 1, 1, 1, 2, 3, 4, 5, 6, 1, 2]
for i in v1[::-1]:
    if i == 1:
        v1.remove(i)
        print(v1)
print(v1)

# 第一次循环 4号索引 [1, 2, 3, 1]
# 第二次循环 3号索引 [1, 2, 3]
# 第三次循环 2号索引 [1, 2, 3]
# 第四次循环 1号索引 [1, 2, 3]
# 第五次循环 0号索引 [2, 3]
```

##### 1.2.5 pop 删除
```python
v1 = ["a", "b", "c", "d"]
res = v1.pop(0)
print(res)
print(v1)

user_list = []
while True:
    user = input("请输入姓名预约抢票(q/Q退出)：")
    if user.upper() == "Q":
        break
    user_list.append(user)

print(user_list)
ticket_num = 3
for i in range(ticket_num):
    success_user = user_list.pop(0)
    print(f"恭喜{success_user}抢票成功！")

print(user_list)
fail_user = "、".join(user_list)
print(f"很遗憾,{fail_user}抢票失败！")
#对于pop来说，如果索引不存在，则会报错
```

##### 1.2.6 clear 清空列表
```python
v1 = ["a", "b", "c", "d"]
v1.clear()
print(v1)
```

##### 1.2.7 reserve 反转原列表
```python
v1 = ["a", "b", "c", "d"]
v1.reverse()
print(v1)
```

##### 1.2.8 index 索引
```python
v1 = ["a", "b", "c", "d"]
print(v1.index("b"))
```

#### 1.3 公共功能
##### 1.3.1 相加
```python
v1 = ["a", "b", "c", "d"]
v2 = [1, 2, 3, 4]
print(v1+v2)
```

##### 1.3.2 相乘
```python
v1 = ["a", "b", "c", "d"]
v2 = [1, 2, 3, 4]
print(v1*3)
```

##### 1.3.3 获取长度
```python
v1 = ["a", "b", "c", "d"]
print(len(v1))
print(v1.__len__())
```

##### 1.3.4 索引，切片
```python
跟字符串相同
```

##### 1.3.5 for循环遍历
```python
v1 = ["a", "b", "c", "d"]
for i in v1:
    print(i)
```

#### 1.4 转换
+ int、bool无法转换成列表
+ str转换为list
+ tuple和dict也可以转换成列表

#### 1.5 一个案例
```python
while True:
    user_input = input("请输入(ip/mask)：")
    ip, mask = user_input.split("/")
    ip_list = ip.split(".")
    ip_bit = ""
    for i in ip_list:
        ip_bit += bin(int(i)).replace("0b", "").zfill(8)
    net_bit = ip_bit[0:int(mask)] + (32-int(mask)) * "0"
    if ip_bit == net_bit:
        print("该地址为网络地址")
    else:
        print("该地址不是网络地址")
```

### 2 tuple 元组
> <font style="color:#000000;">元组（tuple），是一个</font>**<font style="color:#000000;">有序</font>**<font style="color:#000000;">且</font>**<font style="color:#000000;">不可变</font>**<font style="color:#000000;">的容器，在里面可以存放</font>**<font style="color:#000000;">多个不同类型</font>**<font style="color:#000000;">的元素。</font>
>

```python
v1 =  (1) ->属于int
v1 = (1,) ->属于tuple
v1 = (1,2) ->属于tuple
```

#### 2.1 定义
```python
v1 = (11,22,33)
v2 = ("张三","李四")
v3 = (True,123,"张三",[11,22,33,44])

思考：
1. 比较值 v1 = (1) 和 v2 = 1 和 v3 = (1,) 有什么区别？
2. 比较值 v1 = ( (1),(2),(3) ) 和 v2 = ( (1,) , (2,) , (3,),) 有什么区别？

元组本身不可变，但是如果元组当中存在可变类型的元素，那么该元素也可以正常的进行增删改查
v1 = ((1), (2), (3), [1, 2, 3])
print(id(v1))
v1[3].append(4)
print(v1)
print(id(v1))
```



### 3 集合
> 集合是一个 无序 、可变、不允许数据重复的容器。
>

#### 3.1 定义
```python
v1 = {}
print(type(v1))  #字典类型 dict

v1 = {1}
print(type(v1)) #集合类型 set
v1 = set("123")
print(type(v1)) #集合类型
```



+ 无序性

```python
v1 = {"a", "b", "c", "d", 1, 2, 3}
print(v1)
```

+ 可变性

```python
v1 = {"a", "b", "c", "d", 1, 2, 3}
v1.add("e")  #在集合中添加一个元素
print(v1)
v1.discard("f") #在集合中删除一个元素，如果删除的元素不存在，也不会报错
print(v1)
v1.remove("f")  #在集合中删除一个元素，如果删除的元素不存在，则会报错
print(v1)
```

+ 不允许数据重复

```python
l1 = [1, 1, 2, 2, 3, 3]
l2 = []
for i in l1:
    if i in l2:
        pass
    else:
        l2.append(i)
print(l2)

l1 = [1, 1, 2, 2, 3, 3]
s1 = set(l1)
print(s1)
```



#### 3.2 存储原理
> 在创建字典时，首先会生成一张hash表。每次添加一个元素，都会对该元素进行hash的计算，从而得出一个数值，然后存储到hash表相应的位置。正是由于集合这样的存储原理，就要求集合中的元素必须是可hash的
>
> 目前可hash的数据类型：int、bool、str、tuple
>
> 不可hash的数据类型：list set dict
>
> 注意：规律就是看数据类型本身是否是可变的，如果是不可变的即为可hash的数据类型
>

+ 查询速度快

> 由于集合的这种存储特性，使得集合的查询速度会特别快，例如当我们想要查询某个数据时，对于列表而言需要根据索引进行遍历的查找，而对于集合而言，会先对该数据进行一次hash运算，根据hash运算的结果到hash表对应的位置去查找，相当于缩小了查找的范围
>

### 4 字典
> 字典是 **有序**、**键不重复** 且 元素只能是**键值对**的**可变的**  **容器**。
>

+ 字典的元素必须是键值对

这是为了更加精确的索引，在字典中根据键来找到对应的值

+ 字典的键不能够重复，重复会被覆盖

字典中的值就像列表中的元素一样可以相同，而如果想要保持数据的唯一性，则需要为该数据添加独一无二的标识，而字典中的key就可以起到这个作用

+ 无序、有序

在python3.6之前 字典是一种无序的数据类型，而在python3.6之后 字典修正成了一种有序的数据类型

#### 4.1 定义
```python
d1 = {} #定义一个空字典
l1 = ["192.168.1.1", 22, "CE1", "python", "Huawei@123"]
d1 = {"ip": "192.168.1.1", "port": 22, "sysname": "CE1", "username": "python", "password": "Huawei@123"}
```

+ 字典中对于键和值的要求：
+ 键：必须是可哈希的数据类型
+ 值：任意类型

> + 在Python中，可哈希的数据类型指的是那些一旦创建后其值就不能被改变的数据类型。这些数据类型可以作为字典的键或者集合的成员。以下是一些常见的可哈希数据类型：
> 1. **整数 (int)**: 例如 `42`。
> 2. **浮点数 (float)**: 例如 `3.14`。
> 3. **字符串 (str)**: 例如 `"hello"`。
> 4. **元组 (tuple)**: 只要元组内的所有元素都是可哈希的，那么这个元组就是可哈希的。例如 `(1, 2, 'three')`。
> 5. **冻结集 (frozenset)**: 这是一种不可变的集合，因此它是可哈希的。例如 `frozenset([1, 2, 3])`。
> 6. **布尔值 (bool)**: 实际上是整数的一个子类（True 等同于 1，False 等同于 0），因此也是可哈希的。  
值得注意的是，如果一个元组包含任何不可哈希的对象，比如列表或字典，那么这个元组本身也就变得不可哈希了。例如，`(1, [2, 3], 4)` 就不是一个有效的可哈希对象，因为其中包含了列表 `[2, 3]`，而列表不是可哈希的。  
此外，用户自定义的类也可以通过实现 `__hash__()` 方法来变得可哈希，但通常情况下还需要同时实现 `__eq__()` 方法以确保相等性比较和哈希值之间的一致性。对于实现了这两个方法的对象来说，它们必须满足：如果两个对象相等（即 `a == b` 返回 `True`），则它们的哈希值也必须相同（即 `hash(a) == hash(b)`）。反之亦然，如果两个对象不相等，则它们的哈希值应当不同，但这不是强制要求的；不过，如果哈希值不同，确实可以帮助提高哈希表查找效率。
>

+ 字典的使用场景：

当我们想要表示一组固定信息时，用字典可以更加的直观

```python
l1 = ["192.168.1.1", 22, "CE1", "python", "Huawei@123"]
d1 = {"ip": "192.168.1.1", "port": 22, "sysname": "CE1", "username": "python", "password": "Huawei@123"}
```

#### 4.2 字典相关功能方法
##### 4.2.1 由字符串生成字典
```python
device_list = []
for i in range(1, 10):
    ip = f"192.168.1.{i}"
    device_info = {"ip": ip, "device_type": "huawei", "role": "leaf"}
    device_list.append(device_info)#append列表专属

print(device_list)
```

##### 4.2.2 将参数提取出来
```python
device_list = []
for i in range(1, 10):
    ip = f"192.168.1.{i}"
    device_info = {"ip": ip, "device_type": "huawei", "role": "leaf"}
    device_list.append(device_info)

for i in device_list:
    ip = i["ip"]
    device_type = i["device_type"]
    print(f"ip为{ip},设备类型为{device_type}的交换机登录成功")
```

##### 4.2.3 获取所有的键
```python
device_list = []
for i in range(1, 10):
    ip = f"192.168.1.{i}"
    device_info = {"ip": ip, "device_type": "huawei", "role": "leaf"}
    device_list.append(device_info)

for i in device_list:
    print(i.keys())
```

##### 4.2.4 获取所有的值
```python
device_list = []
for i in range(1, 10):
    ip = f"192.168.1.{i}"
    device_info = {"ip": ip, "device_type": "huawei", "role": "leaf"}
    device_list.append(device_info)

for i in device_list:
    print(i.values())
```

##### 4.2.5 获取所有的键值对
```python
device_list = []
for i in range(1, 10):
    ip = f"192.168.1.{i}"
    device_info = {"ip": ip, "device_type": "huawei", "role": "leaf"}
    device_list.append(device_info)

for i in device_list:
    for j in i.items():
        print(j)
```

##### 4.2.6 根据键来修改值
```python
device_list = []
for i in range(1, 10):
    ip = f"192.168.1.{i}"
    device_info = {"ip": ip, "device_type": "huawei", "role": "leaf"}
    device_list.append(device_info)


for i in device_list:
    i["ip"] = "172.168.1.1"
    print(i)
```

##### 4.2.7 清空字典中所有的键值对
```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]
for i in device_info:
    i = {}
    print(i)
```

```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]
for i in device_info:
    i.clear()
    print(i)
```

##### 4.2.8 get获取值
可以通过Get判断一个Key在字典当中是否存在，如果存在则返回该key对应的value，如果不存在，默认返回None。也可以添加一个值，此时如果key不存在则返回添加的值

```python
d1 = {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
res = d1.get("neighbor","no")
print(res)
```

```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]
# 第一次循环{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
# num_info.get("leaf",0)
# num_info = {"leaf":1}
# 第二次循环{"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"}
# num_info.get("spine",0)
# num_info = {"leaf":1,"spine":1}
# 第三次循环{"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"}
# num_info.get("spine",0)
# num_info = {"leaf":1,"spine":2}
num_info = {}
for i in device_info:
    num = num_info.get(i["role"], 0)
    num += 1
    num_info[i["role"]] = num   
print(num_info)
-----------------------------------------------------
leaf_num = 0
spine_num = 0
for i in device_info:
    if i["role"] == "leaf":
        leaf_num += 1
    elif i["role"] == "spine":
        spine_num += 1
num_info = {"leaf": leaf_num, "spine": spine_num}
print(num_info)
```

##### 4.2.9 setdefault设置值
```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]

d1 = {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
d1.setdefault("neighbor", "1.1.1.1")
d1.setdefault("ip", "1.1.1.1")
print(d1)
```

```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]

ip_info = {}
leaf_ip = []
spine_ip = []
for i in device_info:
    if i["role"] == "leaf":
        leaf_ip.append(i["ip"])
    elif i["role"] == "spine":
        spine_ip.append(i["ip"])
ip_info["leaf"] = leaf_ip
ip_info["spine"] = spine_ip
print(ip_info)
```

```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]
# 第一次循环 i= {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
# i["role"] == "leaf   leaf_ip = ["192.168.1.1"]
# 第二次循环 i= {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"}
# i["role"] == "spine"   spine_ip = ["192.168.1.2"]
# 第三次循环 i= {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"}
# i["role"] == "spine"   spine_ip = ["192.168.1.2"，"192.168.1.3"]
# 第三次循环 i= {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}
# i["role"] == "leaf"   leaf_ip = ["192.168.1.1"，"192.168.1.4"]
ip_info = {}
leaf_ip = []
spine_ip = []
for i in device_info:
    if i["role"] == "leaf":
        leaf_ip.append(i["ip"])
    elif i["role"] == "spine":
        spine_ip.append(i["ip"])
ip_info["leaf"] = leaf_ip  #ip_info : {"leaf":["192.168.1.1"，"192.168.1.4"]}
ip_info["spine"] = spine_ip# {'leaf': ['192.168.1.1', '192.168.1.4'], 'spine': ['192.168.1.2', '192.168.1.3']}
print(ip_info)
```

```python
device_info = [{"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"},
               {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.3", "device_type": "huawei", "role": "spine"},
               {"ip": "192.168.1.4", "device_type": "huawei", "role": "leaf"}, ]
# 第一次循环i = {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
# ip_info.setdefault("leaf", [])  ---->ip_info:   {"leaf":[]}
# ip_info["leaf"].append(i["ip"])  ---->ip_info:   {"leaf":["192.168.1.1"]}
#第二次循环i = {"ip": "192.168.1.2", "device_type": "huawei", "role": "spine"}
# ip_info.setdefault("spine", [])  ---->ip_info:   {"leaf":["192.168.1.1"],spine:[]}
# ip_info["spine"].append(i["ip"])  ---->ip_info:   {"leaf":["192.168.1.1"],spine:["192.168.1.2"]}

ip_info = {}
for i in device_info:
    ip_info.setdefault(i["role"], [])
    print(ip_info)
    ip_info[i["role"]].append(i["ip"])
    print(ip_info)
```

##### 4.2.10 update更新字典键值对
```python
d1 = {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
d1.update({"ip": "1.1.1.1"})
d1.update({"neighbor": "1.1.1.1"})
print(d1)
```

##### 4.2.11 pop移除指定键值对
通过key来移除键值对，并且将对应的value返回出来

```python
d1 = {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
res = d1.pop("ip")
print(res)
print(d1)
```

##### 4.2.12 popitem按顺序移除键值对
```python
d1 = {"ip": "192.168.1.1", "device_type": "huawei", "role": "leaf"}
res = d1.popitem()
print(res)
d1.popitem()
d1.popitem()
print(d1)
```

## <font style="color:rgb(59, 59, 59);">什么时候用列表/元组/集合/字典？</font>
<img src="https://cdn.nlark.com/yuque/0/2026/png/50508853/1778466925463-a869de19-d765-41c2-a358-20743a99466b.png" width="804.6666666666666" title="" crop="0,0,1,1" id="u8d68fcd1" class="ne-image">

## <font style="color:rgb(59, 59, 59);">四种结构一句话记忆法</font>
| <font style="color:rgb(59, 59, 59);">结构</font> | <font style="color:rgb(59, 59, 59);">符号</font> | <font style="color:rgb(59, 59, 59);">记法</font> |
| --- | --- | --- |
| <font style="color:rgb(59, 59, 59);">列表 list</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.05);">[]</font>` | **<font style="color:rgb(59, 59, 59);">有序可改</font>**<font style="color:rgb(59, 59, 59);">，像队列，随时能插人踢人</font> |
| <font style="color:rgb(59, 59, 59);">元组 tuple</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.05);">()</font>` | **<font style="color:rgb(59, 59, 59);">有序不可改</font>**<font style="color:rgb(59, 59, 59);">，像身份证号，写死的</font> |
| <font style="color:rgb(59, 59, 59);">集合 set</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.05);">{}</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">无冒号</font> | **<font style="color:rgb(59, 59, 59);">自动去重</font>**<font style="color:rgb(59, 59, 59);">，像门禁名单，重复进不去</font> |
| <font style="color:rgb(59, 59, 59);">字典 dict</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.05);">{key:val}</font>` | **<font style="color:rgb(59, 59, 59);">名字找值</font>**<font style="color:rgb(59, 59, 59);">，像通讯录，用名字查信息</font> |

