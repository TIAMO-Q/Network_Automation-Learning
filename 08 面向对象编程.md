  # 08 面向对象编程
### 1. 何为面向对象
面向对象的核心是一种编程思想，

我们之前在写代码的时候，也一直在采用一种编程思想，就是面向过程的编程编程思想：过程就是流水线，按照需求分步骤解决问题

```python
import random
price_list = ["100元","10元","1000元","10000元"]
while price_list:
    user = input("请输入姓名参与抽奖：")
    prize = random.choice(price_list)
    print(f"恭喜{user}抽到{prize}!")
    price_list.remove(prize)
```

作为编程思想而言，没有好坏之分，只有适不适合

对象其实就是容器，容器是用来存放东西的

对象里面存放的是数据+方法

也就是说，面向对象的核心思想其实就是对数据和方法去做一个整合，只要是满足这样编程的思想，都可以叫做是面向对象的编程思想

### 2. 面向对象基础
#### 2.1 类与对象的定义 #类是对象的模板 对象是类的实例
先定义类 再示例出多个对象

```python
class Animal:
    name = "张三"
    age = 18
    gender = "male"

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")


print(Animal.__dict__["name"])
print(Animal.__dict__["age"])
print(Animal.__dict__["run"])
```

+ 类体代码的执行

类体代码在定义时就会执行，并且会产生命名空间，通过类`类.__dict__`的方法可以看到命名空间中的内容，并且可以通过字典的方式来进行调用



+ 类的属性访问方式

```python
class Animal:
    name = "张三"
    age = 18
    gender = "male"

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")


print(Animal.name)
print(Animal.age)
print(Animal.run)
```





#### 2.2 对象和self
+ 实例化对象的过程

```python
class Animal:
    name = "张三"
    age = 18
    gender = "male"

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    
cat = Animal()
```

1. 创建一个空对象，并且产生对应的命名空间cat = Animal()
2. 对该对象完成实例化操作，为其命名空间添加特有的属性

在python当中  以`__`开头  `__`结尾的方法叫做魔法方法，也就是说在满足特定的情况下会自动调用

其中`__init__`叫做初始化的方法，会在实例化对象时自动调用

3. 将初始化后的对象返回出来

self指的是对象本身，只要携带了self的属性就叫做实例属性，而携带了self的方法，叫做实例方法，实例方法和实例属性都只能通过对象来调用

self可以用来区分不同的对象，在进行实例化操作是，第一个参数传入的是对象本身，这样可以保证属性正确的返回到该对象的命名空间内



#### 2.3 常见成员
+ 实例变量：属于对象，只能通过对象调用
+ 实例方法：属于对象，只能通过对象来调用
+ 类方法：属于类，通过对象调用或通过类来调用
+ 类变量：属于类，可以通过对象或者类来调用

```python
class Animal:
    name = "张三"
    age = 18
    gender = "male"

    def __init__(self,name,age,gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")


print(Animal.__dict__)
cat = Animal(name="cat",age="8",gender="male")
print(cat.__dict__)
cat.jump()
```

通过该例可以看出，对象里面只保存属性，而方法一律保存在类的命名空间当中。

当对象调用实例方法时，会直接从类的命名空间中调用。

```python
class Animal:
    name = "张三"
    age = 18
    gender = "male"

    def __init__(self, name, age, gender, money):
        self.name = name
        self.age = age
        self.gender = gender
        self.money = money

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    def play_the_field(self):
        if self.money == "more":
            print("请挑选嫩模")
        elif self.money == "less":
            print("穷逼不配拥有爱情")


zhangsan = Animal(name="张三", age=18, gender="male", money="more")
lisi = Animal(name="李四", age=18, gender="male", money="less")
zhangsan.play_the_field()
lisi.play_the_field()
```



#### 2.4 基本应用场景
+ 存储数据

```python
switch_info = {"CE1": {"ip": "1.1.1.1", "port": 22},
               "CE2": {"ip": "1.1.1.2", "port": 22},
               "CE3": {"ip": "1.1.1.3", "port": 22}, }
print(switch_info["CE1"]["ip"])


class SwitchInfo:
    def __init__(self, ip, port):
        self.ip = ip
        self.port = port


CE1 = SwitchInfo(ip="1.1.1.1", port=22)
CE2 = SwitchInfo(ip="1.1.1.2", port=22)
CE3 = SwitchInfo(ip="1.1.1.3", port=22)
print(CE1.ip)
```

+ 初始化时先对数据进行相应的处理

```python
import os


class File:
    def __init__(self):
        if not os.path.exists("files"):
            os.makedirs("files")

    def create_file(self, filename):
        with open(f"files/{filename}", "w", encoding="utf-8") as f:
            pass


a = File()
a.create_file(filename="abc.txt")
```

+ 再看数据类型

```python
l1 = list([1, 2, 3, 4]) #实例化对象
l1.append(666) #调用实例方法
```

### 3.三大特性
#### 3.1 封装
封装主要体现在两个方面：

+ 将同一类方法封装到了一个类中



+ 将数据封装到了对象中，在实例化一个对象时，可以通过`__init__`初始化方法在对象中封装一些数据，便于以后使用

#### 3.2 继承
+ 什么是继承？

现实生活当中，存在很多继承的概念。但是继承的方式有所不同

1.继承财产，继承家业

关于继承可以有两种说法：

2.继承传统手艺

```python
父类和子类
基类和派生类
```

+ 为什么需要继承

为了避免重复造轮子

假设说已经定义了一个动物类 ，在动物类的基础上可以进一步的细分：哺乳类、家禽类。那此时不必从0开始去定义这两个类，而是在原有的动物类的基础上直接进行继承即可

+ 经典类和新式类

新式类就是默认继承了object的类

经典类就是默认没有继承object的类

目前我们使用的都是新式类，默认会继承object

可以这么理解，object是所有类的祖宗

##### 3.2.1 一般继承
```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")


class Humanbeing(Animal):
    pass


zhangsan = Humanbeing(name="张三", age=18, gender="male")
print(zhangsan.name)
zhangsan.run()
```



##### 3.2.2 多层继承
```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")


class Intelligence(Animal):
    def study(self):
        print("我会学习")

    def control(self):
        print("我能控制自己")


class Humanbeing(Intelligence):
    pass


zhangsan = Humanbeing(name="张三", age=18, gender="male")
zhangsan.study()
zhangsan.run()
```

+ 方法的重写

当使用多层继承时，对象在调用方法时，会先从自己对应的类的上一级进行查找

如果说在上级和上上级的类里面拥有相同的方法，那么回去调用上级里面的方法

这个叫做方法的重写

```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    def control(self):
        print("我不能控制自己")


class Intelligence(Animal):
    def study(self):
        print("我会学习")

    def control(self):
        print("我能控制自己")


class Humanbeing(Intelligence):
    pass


zhangsan = Humanbeing(name="张三", age=18, gender="male")
zhangsan.study()
zhangsan.control()
```

+ super代表是当前类的父类

```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    def control(self):
        print("我不能控制自己")


class Intelligence(Animal):
    def study(self):
        print("我会学习")

    def control(self):
        print("我能控制自己")


class Humanbeing(Intelligence):
    def control(self):
        super(Intelligence,self).control()


zhangsan = Humanbeing(name="张三", age=18, gender="male")
zhangsan.control()
```

```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    def control(self):
        print("我不能控制自己")


class Intelligence(Animal):
    def study(self):
        print("我会学习")



class Humanbeing(Intelligence):
    pass


zhangsan = Humanbeing(name="张三", age=18, gender="male")
zhangsan.control()
```

##### 3.2.3 多重继承
```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    # def control(self):
    #     print("我不能控制自己")


class Intelligence(object):
    def study(self):
        print("我会学习")

    def control(self):
        print("我能控制自己")



class Humanbeing(Animal,Intelligence):
    pass


zhangsan = Humanbeing(name="张三", age=18, gender="male")
zhangsan.control()
```

如果此时想要调用右边类当中的方法，可以在当前类中进行方法的重写

```python
class Animal(object):
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    def run(self):
        print("我会跑")

    def jump(self):
        print("我会跳")

    def control(self):
        print("我不能控制自己")


class Intelligence(object):
    def study(self):
        print("我会学习")

    def control(self):
        print("我能控制自己")



class Humanbeing(Animal,Intelligence):
    def control(self):
        Intelligence.control(self)


zhangsan = Humanbeing(name="张三", age=18, gender="male")
zhangsan.control()
```



+ 继承的顺序问题
1. 执行对象`.`方法时，优先去当前对象所关联的类中找，没有找到才接着去父类中查找
2. 在多重继承中，先继承左边的，再继承右边的
3. 搞清楚self到底是谁，首先去self对应的那个类中去获取成员，没有就按照继承关系向上查找



##### 3.2.4 钻石继承
###### 3.2.4.1 什么是钻石继承
<img src="https://cdn.nlark.com/yuque/0/2026/png/50508853/1779025260761-7683df4f-bef6-4311-82bd-5cc7e15607e5.png" width="274" title="" crop="0,0,1,1" id="gfbs7" class="ne-image">

假设此时B跟C继承了A，然后D又继承了B和C，此时就形成了一个钻石继承

```python
class A:
    # def f1(self):
    #     print(1)

    def f2(self):
        print(2)

    def f3(self):
        print(3)

    def f4(self):
        print(4)


class B(A):
    # def f1(self):
    #     print(11)

    def f2(self):
        print(22)

    def f3(self):
        print(33)

    def f4(self):
        print(44)


class C(A):
    # def f1(self):
    #     print(111)

    def f2(self):
        print(222)

    def f3(self):
        print(333)

    def f4(self):
        print(444)


class D(B, C):
    # def f1(self):
    #     print(1111)

    def f2(self):
        print(2222)

    def f3(self):
        print(3333)

    def f4(self):
        print(4444)


a = D()
print(D.mro())
```



###### 3.2.4.2 C3算法和MRO列表
通过C3算法可以得到一个类的继承顺序

通过类.mro()的方式可以看到该继承顺序

```python
print(D.mro())
```



##### 3.2.5 Mixint机制
Mixint准确来讲是一种规范，为的是让代码更贴合人的逻辑

```python
class Animal:
    pass

class EggMixin:
    def egg_func(self):
        pass

class Duck(EggMixin,Animal):
    pass

class Chicken(EggMixin,Animal):
    pass

class Cat(Animal):
    pass
```



#### 3.3 多态
+ 什么是多态？

多态：一种事物的多种形态，比如说水：气态（水蒸气），液态（水），固态（冰）

```python
class Animal:
    def speak(self):
        print("我是动物，我会叫")


class Cat(Animal):
    def speak(self):
        super().speak()
        print("我是猫，我的叫声是喵喵喵")

class Dog(Animal):
    def speak(self):
        super(Dog, self).speak()
        print("我是狗，我的叫声是汪汪汪")

class Humanbeing(Animal):
    def speak(self):
        super(Humanbeing, self).speak()
        print("我是人，我的叫声是啊啊啊")


a = Dog()
a.speak()
```



+ 鸭子类型

起源于一句古谚语：如果有一个人，走起路来像鸭子，叫起来像鸭子，游起泳来也像鸭子，那么他就是鸭子

比如在linux中，有一个非常重要的概念：一切皆文件

那么cpu是不是文件  硬盘是不是文件  网卡是不是文件，但是只要我们把这些硬件做的像文件，那么我们就可以把它理解成文件

这样做的好处时，只要我们掌握了文件的操作（文件的增删改查），理所当然的就可以去操作这些硬件



+ 扩展：抽象基类

```python
import abc


class Animal(metaclass=abc.ABCMeta):
    @abc.abstractmethod
    def speak(self):
        print("我是动物，我会叫")


class Cat(Animal):
    def speak(self):
        pass
    # def speak(self):
    #     super().speak()
    #     print("我是猫，我的叫声是喵喵喵")

class Dog(Animal):
    def speak(self):
        super(Dog, self).speak()
        print("我是狗，我的叫声是汪汪汪")

class Humanbeing(Animal):
    def speak(self):
        super(Humanbeing, self).speak()
        print("我是人，我的叫声是啊啊啊")


a = Cat()
a.speak()
```



### 4.隐藏属性f
#### 4.1 什么是隐藏属性？
在我们之前学习属性的调用时，一般都是通过对象.属性的方式进行调用，那么如果我们想要将这个属性隐藏起来，在定义属性的时候可以在前面加上__ ，此时该属性就变成了一个隐藏属性，使用原有的调用方法无法调用成功

```python
class Foo:
    def __init__(self, x):
        self.__x = x

    def func(self):
        pass


a = Foo(1)
print(a.__x)
```

```python
"""
隐藏属性本质上其实是对名字的一个变形，将__x 变形成`_Foo__x`
__name 变形成了_Foo__name
使用对象.（变形后的变量名）来进行调用是可以调用到值的
"""
class Foo:
    __name = "张三"

    def __init__(self, x):
        self.__x = x

    def func(self):
        pass


# print(Foo._Foo__name)
a = Foo(1)
print(a._Foo__x)
```

#### 4.2 为什么需要隐藏属性？
就是让用户不能通过常规的方式的来调用，而是通过开发者自定义的接口来进行调用，然后可以在该接口下添加任意的逻辑

```python
class Foo:

    def __init__(self, x):
        self.__x = x

    def get_x(self):
        print(self.__x)  # --->_Foo__x

    def set_x(self, val):
        if val != str(val):
            print("传入的值必须为字符串")
        else:
            self.__x = val # _Foo__x = val
            
    def del_x(self):
        del self.__x


a = Foo(1)
a.set_x("666")
a.get_x()
```



### 5. 成员相关补充
+ 变量
    - 实例变量
    - 类变量
+ 方法
    - 绑定方法
    - 类方法
    - 静态方法

#### 5.1 类方法
如果有一些方法，需要直接通过类来进行调用，就可以使用@classmethod将该方法变为一个类方法，此时传入的第一个参数是类本身

```python
from config import SWITCH_INFO
class Foo:
    name = "zhangsan"

    def __init__(self, sysname, ip, port, username, password):
        self.sysname = sysname
        self.ip = ip
        self.port = port
        self.username = username
        self.password = password

    @classmethod# 加了@classmethod之后，该方法为类方法，此时传入的第一个参数是类本身
    def create_obj(cls,var):
        return cls(sysname=SWITCH_INFO[var]["sysname"],ip=SWITCH_INFO[var]["ip"],
                port=SWITCH_INFO[var]["port"],username=SWITCH_INFO[var]["username"],
                password=SWITCH_INFO[var]["password"])



CE1 = Foo.create_obj("CE1")
print(CE1.__dict__)
# print(type(CE1),CE1)

# a = Foo("CE1","192.168.1.1",22,"python","Huawei@123")
# obj = Foo.create_obj(var=123,cls=Foo)
# print(obj.__dict__)
```

#### 5.2 静态方法
静态方法实际上也没有一个确切的应用场景，如果在编程期间，有一个方法既不想通过对象来调用也不想通过类来调用，那么就可以将其设置为静态方法

```python
from config import SWITCH_INFO
from datetime import datetime
class Foo:
    name = "zhangsan"

    def __init__(self, sysname, ip, port, username, password):
        self.sysname = sysname
        self.ip = ip
        self.port = port
        self.username = username
        self.password = password

    @staticmethod  # showtime = staticmethod(showtime)
    def showtime():
        now = datetime.now()
        print(now)

    showtime()

print(Foo.__dict__)
```

#### 5.3 property
最大的作用其实就是改变原有方法的调用方式，可以通过对象.函数名的方式直接进行调用



+ 属性的编写方式：
1. 基于定义变量

```python
# 本质上是一种装饰器，可以让我们用调用属性的方式来调用方法

class Foo:
    def __init__(self, x):
        self.__x = x

    def get_x(self):
        print(self.__x)

    def set_x(self, val):
        self.__x = val

    def del_x(self):
        del self.__x

    x = property(fget=get_x, fset=set_x, fdel=del_x)


obj = Foo(1)
obj.x
obj.x = 2
del obj.x
obj.x
```



2. 基于装饰器

```python
class Foo:
    def __init__(self, x):
        self.__x = x

    @property
    def x(self):
        print(self.__x)

    @x.setter
    def x(self, val):
        self.__x = val

    @x.deleter
    def x(self):
        del self.__x




obj = Foo(1)
obj.x = 2
obj.x
# del obj.x
# obj.x
```

```python
# 本质上是一种装饰器，可以让我们用调用属性的方式来调用方法

class Foo:
    def __init__(self, x):
        self.__x = x

    @property
    def x(self):
        return 1000

    @x.setter
    def x(self, val):
        self.__x = val

    @x.deleter
    def x(self):
        del self.__x

    def func(self):
        print(self.x+self.x)




obj = Foo(1)
obj.func()
# del obj.x
# obj.x
```



### 6.反射
#### 6.1 反射的基本原理
通过字符串的方式来访问对象当中的属性和方法

```python
class Foo:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def f1(self):
        print(123)

    def f2(self):
        print(456)


obj = Foo(name="zhangsan", age=18)
# 利用字符串判断该属性是否存在
# res = hasattr(obj,"nameasfas")
# print(res)

# 通过字符串判断属性是否存在,如果属性名存在则返回对应的值，不存在则返回定义的默认值
# res = getattr(obj,"f1",None)
# res()

# 通过字符串的方式来修改属性值
# setattr(obj,"name","lisi")
# res = getattr(obj,"name")
# print(res)

# 通过字符串的方式来删除属性值
# delattr(obj,"name")
# res = getattr(obj,"name",None)
# print(res)
```

```python
class Foo:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def f1(self):
        print(123)

    def f2(self):
        print(456)

    def choose_func(self):
        user_input = input("请输入需要执行的方法:")
        if hasattr(self,user_input):
            res = getattr(self,user_input)
            res()
        else:
            print("该方法不存在")

obj = Foo(name="zhangsan", age=18)
obj.choose_func()
```

#### 6.2 import_module + 反射
+ import_module 通过字符串的形式来导入模块，注意：最多只能导入到模块级别

```python
from importlib import import_module
cont = "time"
asd = import_module(cont)
print(asd.time())
```

```python
from importlib import import_module

asd = import_module("5-20.config") # 模块级别
print(asd.SWITCH_INFO)

asd = import_module("config.SWITCH_INFO") # 非模块级别，错误的导入
print(asd)
```



+ import_module + 反射 案例

```python
from importlib import import_module
from config import INFO


class SwitchMonitor:
    def __init__(self, ip):
        self.ip = ip

    def cpu_monitor(self):
        cpu_state = "fault"
        if cpu_state == "fault":
            for i in INFO:
                m, c = i.split(".")
                module = import_module(m)
                cls = getattr(module, c, None)
                obj = cls()
                obj.send()

    def fan_monitor(self):
        pass

    def power_state(self):
        pass

obj = SwitchMonitor(ip="192.168.1.1")
obj.cpu_monitor()
```

+ config文件

```python
INFO = [
    "email.Email",
    "wechat.WeChat",
    "qq.QQ",
    "phone.Phone"
]
```

+ 其他py文件

```python
class QQ:
    def send(self):
        print("发送QQ告警")
        
class Email:
    def send(self):
        print("发送邮件告警")

class WeChat:
    def send(self):
        print("发送微信告警")
```
