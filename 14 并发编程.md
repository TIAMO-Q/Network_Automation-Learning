# 并发编程
### 1. 操作系统发展史
操作系统的发展史，本质上就是不断地提高CPU利用率的过程

### 2. 多道技术
单核实现并发的效果

1.I/O的操作（输入，输出，网络传输）

2.计算数据

#### 2.1 串行
根据顺序，依次执行，CPU的利用率非常低

#### 2.2 并发和并行
+ 并发

看起来像是在同时执行，就叫做并发

+ 并行

实际上的确在同时执行，就叫做并行

所以，并行一定可以叫做并发，但是并发不一定叫做并行

#### 2.3 复用
+ 空间上的复用

多个应用程序共用一套硬件

+ 时间上的复用

在同一时间，同时做着很多事情：点外卖，洗衣服，学习

#### 2.4 cpu是如何切换的
1.I/O操作的时候，主动释放CPU的使用权限

2.长时间占用，剥夺CPU的使用权限

### 3.进程的调度
#### 1. 进程与程序的区别
程序是死的，进程是活的

+ 程序

程序实际就相当于一大段功能代码的集合

+ 进程

进程指的就是程序实际执行的过程

#### 2. 进程调度
当多个进程同时执行时，需要分配CPU的资源，而这个分配过程不是随机的，而是遵循一定的规则，那这个规则就需要进程的调度算法

+ 先来先服务调度算法：对长作业有利
+ 短作业优先调度算法：对短作业有利
+ 时间片轮转法+多级反馈队列



### 4.进程与线程概述
#### 4.1 进程和线程的概念
+ 进程指的就是程序执行的过程，线程是进程工作的最小单位

假设应用程序相当于是一个工厂（生产东西-数据的），此时进程就相当于是工厂的车间，而线程就相当于是车间的工人，因此每个进程最少要存在一个线程

此时如果我们想要提高生产效率：1.扩车间 2.每个车间增加工人

进程和线程分别是在什么的基础上创建出来的？

进程是在程序的基础上申请额外的内存空间

线程是在进程的基础上增加线程

因此进程是比线程消耗资源更多的方式

#### 4.3 进程的创建
```python
import time
from multiprocessing import Process


# Process 是内置模块

def func(num):
    for i in range(num):
        pass
    print(time.time())


# 24s--->12s
# 在windows系统下，创建进程必须在if __name__ == '__main__':下
# 这是因为在windows系统下，创建进程是以模块导入的形式进行创建
# 如果不在if __name__ == '__main__':创建，每次导入都会重新创建一个进程，不断循环

if __name__ == '__main__':
    num_list = [1000000000, 1000000000]
    print(time.time())
    for i in num_list:
        p = Process(target=func, args=(i,))  # 创建进程，注意target接的参数不要带括号，同时args跟的参数要加逗号（元组）
        p.start() # 启动进程
    # 在子进程执行的过程当中，主进程不会等到子进程执行完毕
    # print(time.time())
```

+ join方法

通过该方法，可以让主进程等待子进程结束之后再执行

```python
import time
from multiprocessing import Process


# Process 是内置模块

def func(num):
    for i in range(num):
        pass


# 24s--->12s
# 在windows系统下，创建进程必须在if __name__ == '__main__':下
# 这是因为在windows系统下，创建进程是以模块导入的形式进行创建
# 如果不在if __name__ == '__main__':创建，每次导入都会重新创建一个进程，不断循环

if __name__ == '__main__':
    num_list = [1000000000, 1000000000]
    print(time.time())
    for i in num_list:
        p = Process(target=func, args=(i,))  # 创建进程，注意target接的参数不要带括号，同时args跟的参数要加逗号（元组）
        p.start() # 启动进程
        p.join()  # 使用了join方法之后，代表主进程必须等待该进程结束之后才能执行
    # 在子进程执行的过程当中，主进程不会等到子进程执行完毕
    print(time.time())
```

问题：在使用join方法时，如果让多个子进程同时执行

```python
import time
from multiprocessing import Process


# Process 是内置模块

def func(num):
    for i in range(num):
        pass


# 24s--->12s
# 在windows系统下，创建进程必须在if __name__ == '__main__':下
# 这是因为在windows系统下，创建进程是以模块导入的形式进行创建
# 如果不在if __name__ == '__main__':创建，每次导入都会重新创建一个进程，不断循环

if __name__ == '__main__':
    num_list = [1000000000, 1000000000]
    p_list = []
    print(time.time())
    for i in num_list:
        p = Process(target=func, args=(i,))  # 创建进程，注意target接的参数不要带括号，同时args跟的参数要加逗号（元组）
        p_list.append(p)
        p.start() # 启动进程  # 使用了join方法之后，代表主进程必须等待该进程结束之后才能执行

    for i in p_list:
        i.join()

    print(time.time())

# 在使用join方法时，如果让多个子进程同时执行
```

#### 4.3 进程互斥锁
+ 当多个进程同时操作一份数据时，会出现数据错乱的情况

```python
import json
import random
import time
from multiprocessing import Process


def search(name):
    with open("config.txt", "r", encoding="utf-8") as f:
        dic = json.load(f)
    print("用户{}查看当前余票为{}".format(name, dic.get("ticket_num")))


def buy(name):
    search(name)
    time.sleep(random.randint(1, 3))
    with open("config.txt", mode="r", encoding="utf-8") as f:
        dic = json.load(f)

    if dic.get("ticket_num") > 0:
        print("用户{}抢票成功！".format(name))
        dic["ticket_num"] -= 1
        with open("config.txt","w",encoding="utf-8") as f:
            json.dump(dic,f)
    else:
        print("用户{}抢票失败！".format(name))

if __name__ == '__main__':
    for i in range(1,11):
        p = Process(target=buy,args=(i,))
        p.start()
```

+ 解决办法：添加互斥锁：就是将原有的并行变成串行，牺牲了执行效率，但是保护了数据安全

```python
import json
import random
import time
from multiprocessing import Process,Lock


def search(name):
    with open("config.txt", "r", encoding="utf-8") as f:
        dic = json.load(f)
    print("用户{}查看当前余票为{}".format(name, dic.get("ticket_num")))


def buy(name,mutex):
    search(name)
    time.sleep(random.randint(1, 3))
    mutex.acquire()  # 抢锁
    with open("config.txt", mode="r", encoding="utf-8") as f:
        dic = json.load(f)

    if dic.get("ticket_num") > 0:
        print("用户{}抢票成功！".format(name))
        dic["ticket_num"] -= 1
        with open("config.txt", "w", encoding="utf-8") as f:
            json.dump(dic, f)
    else:
        print("用户{}抢票失败！".format(name))
    mutex.release() # 释放锁


if __name__ == '__main__':
    mutex = Lock()
    for i in range(1, 11):
        p = Process(target=buy, args=(i,mutex))
        p.start()
```

##### 4.3.1 进程之间数据互相隔离
```python
from multiprocessing import Process

num = 100


def func():
    global num
    num = 666


if __name__ == '__main__':
    p = Process(target=func)
    p.start()
    p.join()
    print(num)
```



#### 4.4 线程的创建
```python
from threading import Thread
import time


def func(num):
    for i in range(num):
        pass

# 21s
# 23s
if __name__ == '__main__':
    num_list = [1000000000, 1000000000]
    t_list = []
    print(time.time())
    for i in num_list:
        func(i)
        t = Thread(target=func,args=(i,))
        t_list.append(t)
        t.start()
    for i in t_list:
        i.join()

    print(time.time())
```

通过这个例子可以看到，通过多线程的方式没有办法提高程序的执行效率，没有缩短程序的执行时间，这是因为有GIL锁的存在

#### 4.5 GIL锁
+ GIL锁其实是CPython当中设计的一个功能，它的目的就是让一个进程当中同一时间最多只有一个线程能被CPU调度
+ 什么时候使用多进程，什么时候使用多线程？

GIL锁让多线程丧失了多核优势，但是保证了数据安全

但并不是因为这样，多线程就没有了意义

I\O密集型：使用多线程，更多的文件存取，更多的网络传输

计算密集型：使用多进程，计算大量的数据

##### 4.5.1 同个进程的线程数据共享
```python
from threading import Thread

num = 100


def func():
    global num
    num = 666


if __name__ == '__main__':
    t = Thread(target=func)
    t.start()
    t.join()
    print(num)
```

```python
from threading import Thread

num = 100

# 抢GIL锁
def func():
    global num
    n = num
    num = n-1
# 释放GIL锁


if __name__ == '__main__':
    t_list = []
    for i in range(100):  # 开启了100个线程
        t = Thread(target=func)
        t_list.append(t)
        t.start()
    for i in t_list:
        i.join()
```



#### 4.6 线程互斥锁
```python
import time
from threading import Thread

num = 100

# 抢GIL锁
def func():
    global num
    n = num
    time.sleep(1) # I/O操作
    num = n-1



if __name__ == '__main__':
    t_list = []
    for i in range(100):  # 开启了100个线程
        t = Thread(target=func)
        t_list.append(t)
        t.start()
    for i in t_list:
        i.join()

    print(num)
```

```python
import time
from threading import Thread, Lock

num = 100

mutex = Lock()


def func():
    global num
    mutex.acquire()
    n = num
    time.sleep(0.1)  # I/O操作
    num = n - 1
    mutex.release()


"""
同时开启100个线程，此时每个线程要去抢夺两把锁，一把是GIL锁，一把是互斥锁
此时线程遇到I/O操作会释放GIL锁，但是不会释放互斥锁
必须等线程执行完毕才会释放互斥锁
"""

if __name__ == '__main__':
    t_list = []
    print(time.time())
    for i in range(100):  # 开启了100个线程
        t = Thread(target=func)
        t_list.append(t)
        t.start()
    for i in t_list:
        i.join()

    print(time.time())
    print(num)
```

#### 4.7 并发的实现网络编程
+ server

```python
import socket
import subprocess
import struct
from threading import Thread

# 1.买服务器
# AF_INET 标识了socket的地址族，是用于网络传输的
# SOCK_STREAM 标识协议的类型，而TCP协议是一种典型的流式协议
# 此时的server就定义了一个用于网络传输，并且采用TCP协议的服务器
server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
# 2.绑定网卡
server.bind(("127.0.0.1", 8081))
# 3.开机
# listen标识了半连接池的容量，此时最多可以容纳5个连接请求
server.listen(5)


# 4.监听连接请求
# (1) 从半连接池当中取出一个链接请求交给accept处理，建立链接
# (2) accept处理完链接请求得到的返回值为一个元组，分别代表真实
#     建成的连接，以及该连接中客户端具体的IP地址信息
def func(conn):
    while True:
        cmd = conn.recv(1024).decode("utf-8")
        if cmd.__len__() == 0: break
        if cmd.upper() == "Q": break
        obj = subprocess.Popen(args=cmd,
                               shell=True,
                               stdout=subprocess.PIPE,
                               stderr=subprocess.PIPE
                               )
        std_out = obj.stdout.read()
        std_err = obj.stderr.read()
        result = std_out + std_err
        total_size = struct.pack('i', len(result))
        # # 6. 发送消息，使用send方法，只能发从字节类型的数据
        conn.send(total_size)
        conn.send(result)
    conn.close()


while True:
    conn, client_addr = server.accept()
    t = Thread(target=func, args=(conn,))
    t.start()

```

+ client

```python
import socket
import struct

# 1. 产生一个客户端
client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
# 2. 发起连接请求
client.connect(("127.0.0.1", 8081))
# 3. 发信息
while True:
    user_input = input("请输入需要执行的命令(q/Q退出):").strip()
    if user_input.__len__() == 0: continue
    client.send(user_input.encode("utf-8"))
    if user_input.upper() == "Q": break
    # 4. 收信息
    total_size = struct.unpack('i', client.recv(4))[0]
    size = 0
    res = ""
    while size < total_size:
        res += client.recv(1024).decode("GBK")
        size += 1024
    print(res)

# 5. 关闭客户端
client.close()
```
