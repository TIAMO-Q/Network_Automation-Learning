## 1. 函数的作用域
### 1.1 初识作用域
+ 在python当中，是以函数作为作用域的，在不同函数中生成和创建的变量，所能够使用的范围也只能在函数包含的代码块内。不同函数之间的变量互不相关，也不能互相调用

```python
def f1():
    name = "张三"
    print(name)


def f2():
    name = "李四"
    print(name)


f1()
f2()
```

+ 在同一个函数内，也就是在一个作用域内，变量是可以互相调用的，但需要注意变量必须先要被定义

```python
def f1():
    num = input("请输入一个数字：")
    if int(num) > 5:
        name = "张三"
    print(name)

f1()
```



### 1.2 全局作用域和局部作用域
通俗意义上来讲，定义在函数内部的变量叫做局部变量，作用域为局部作用域。定义在函数外部的变量叫做全局变量，作用域为全局作用域。

全局作用域下无法调用局部作用域的变量，但是在局部作用域下可以调用全局作用域下的变量

```python
name = "张三"
age = 18


def f1():
    age = 20
    gender = "male"
    print(name)
    print(age)
    print(gender)


f1()
```

在函数内部调用变量时，首先从函数内部的局部作用域下进行查找，如果查找不到就从上级的作用域下进行查找

```python
name = "张三"
age = 18


def f1():
    age = 20
    gender = "male"
    print(name)
    print(age)
    print(gender)


f1()
print(name)
print(age)
print(gender)
```



### 1.3 global关键字
+ 根据之前的内容，我们知道，在局部作用域中可以使用全局变量。但是在局部作用域中是无法修改非可变的全局变量的

```python
data_list = [1, 2, 3]


def change_list():
    data_list.append(4)
    print(data_list)


change_list()
print(data_list)
```

```python
name = "张三"
age = 18


def f1():
    global name
    name = "李四"
    age = 20
    gender = "male"
    print(name)



f1()
print(name)
```

规范要求，全局变量一般为大写

### 1.4 nolocal 关键字
```python
name = "王二"
def outer():
    # name = "张三"

    def inner():
        # name = "李四"
        print(name)

    inner()


outer()
```

```python
name = "王二"
def outer():
    name = "张三"

    def inner():
        nonlocal name
        name = "李四"
        print(name)

    inner()
    print(name)


outer()
```

```python
name = "王二"
def outer():
    name = "张三"

    def inner():
        global name
        name = "李四"
        print(name)

    inner()
    print(name)


outer()
print(name)
```



## 2. 函数的嵌套
### 2.1 什么是嵌套
+ 函数在定义和调用时跟变量一样存在作用域的问题

```python
def outer():
    def inner():
        print(123)
        return 456
    res = inner()
    return res


res = outer()
print(res)
```

+ 在函数内部也同样可以定义函数，就相当于局部变量一样，只有在函数内部作用域中才能调用，也就是函数的嵌套

```python
def outer():
    def inner():
        print(123)
        return 456
    return inner


res = outer()
v1 = res()   #---->inner()
print(v1)
```

+ 为什么要做函数的嵌套

在共同开发项目时，为了避免函数名相同的干扰，而又有功能分解的需要，就可以在函数的内部来定义函数，这样在函数内部去调用的时候，就只会调用作用域内的函数

```python
def inner():
    print(789)


def inner():
    print(456)


def outer():
    def inner():
        print(123)

    inner()


outer()
```



### 2.1 嵌套相关的作用域问题
作用域不是在定义的时候出现的，而是在执行的时候才会出现

```python
name = "马琨琪"


def outside():
    name = "健康哥"

    def inner():
        print(name)

    inner()
outside()
```

```python
name = "马琨琪"


def outside():
    name = "健康哥"

    def inner():
        print(name)

    return inner


v1 = outside()
v1()
```

```python
def outside():
    name = "健康哥"

    def inner():
        print(name)

    return [inner, inner]


v1 = outside()
v1[1]()
v1[2]()
```

+ 多层嵌套函数

```python
def outer():
    def inner():
        def f1():
            return 123

        return f1

    return inner


v1 = outer()
v2 = v1()
res = v2()
print(res)
```

## 3. 闭包
### 3.1 闭包的基本概念
> python中的闭包从表现形式上定义为：如果在一个内部函数里，对在外部作用域但不是在全局作用域的变量进行引用（简言之：就是在嵌套函数的环境下，内部函数引用了外部函数的局部变量），那么内部函数就被认为是闭包
>

```python
def outer(a1, a2):
    
    def inner():
        return a1 + a2


outer(10, 20)
```

```python
def outer(a1, a2):
    def inner():
        return a1 + a2

    return inner


res = outer(10, 20)
v1 = res()
print(v1)
```

### 3.1 闭包的应用场景
+ 闭包一个很大的应用场景就在于尽可能的避免使用全局变量，以免对全局作用域产生污染，外部函数可以为内部函数提供一个封闭的环境，调用外部函数时传入相应的参数生成变量，内部函数可以引用外部函数生成的局部变量



## 4. 装饰器
> 在python中装饰器（decorator）的功能是将被装饰的函数当作参数传递给与装饰器对应的函数（名称相同的函数），并返回包装后的被装饰的函数
>

+ 第一种场景

```python
# 第一个场景

def login():
    print("交换机正在登录...")

# 此时假设要对原有的函数做出功能上的增强
# 1.
def login():
    print("验证用户名密码...")
    print("交换机正在登录...")
    print("登录成功..")

login()
# 2.
def login():
    print("交换机正在登录...")


def outer(func):
    def inner():
        print("验证用户名密码...")
        func()
        print("登录成功..")

    return inner


v1 = outer(login)
v1()
```

+ 第二种场景

```python
1.
def login():
    print("验证用户名密码...")
    print("交换机正在登录...")
    print("登录成功..")

login()

2.
def outer(func):
    def inner():
        print("验证用户名密码...")
        func()
        print("登录成功..")

    return inner
# @ 语法糖：在不改变原有代码的含义的基础上，进一步的简化代码
@outer #login = outer(login)  
def login():
    print("交换机正在登录...")


login()
```

+ 第三种场景

```python
# 第三种场景
# 当有多个函数都需要功能的增强时

# def login1():
#     print("验证用户名密码...")
#     print("交换机正在登录...")
#     print("登录成功..")
#     return "huawei"
#
#
# def login2():
#     print("验证用户名密码...")
#     print("交换机正在登录...")
#     print("登录成功..")
#     return "cisco"
#
#
# def login3():
#     print("验证用户名密码...")
#     print("交换机正在登录...")
#     print("登录成功..")
#     return "H3C"


# ================================
def outer(func):
    def inner():
        print("验证用户名密码...")
        func()
        print("登录成功..")

    return inner


@outer  # login1 = outer(login1)
def login1():
    print("交换机正在登录...")
    return "huawei"


@outer
def login2():
    print("交换机正在登录...")
    return "cisco"


@outer
def login3():
    print("交换机正在登录...")
    return "H3C"


login1()
login2()
login3()
```

+ 第四种场景

```python
def outer(func):
    def inner(*args, **kwargs):
        print("验证用户名密码...")
        res = func(*args, **kwargs)
        print("登录成功..")
        return res

    return inner


@outer  # login1 = outer(login1)
def login1(a1):
    print("交换机正在登录...")
    return "huawei"


@outer
def login2(a1, a2):
    print("交换机正在登录...")
    return "cisco"


@outer
def login3(a1, a2, a3):
    print("交换机正在登录...")
    return "H3C"


v1 = login1(1)
print(v1)
v2 = login2(1, 2)
print(v2)
v3 = login3(1, 2, 3)
print(v3)
```

+ 第五种场景

```python
def func():
    """
    这是一个func函数
    """


print(func.__name__)#返回函数名
print(func.__doc__) #返回注释内容

--------------------------------------------------------

def outer(func):
    @functools.wraps(func)   #将inner.__name__ inner.__doc__变为func.__name__和func.__doc__
    def inner(*args, **kwargs):
        """
        这是一个inner函数
        """
        print("验证用户名密码...")
        res = func(*args, **kwargs)
        print("登录成功..")
        return res

    return inner


@outer  # login1 = outer(login1)
def login1(a1):
    """
    这是login1函数
    """
    print("交换机正在登录...")
    return "huawei"


print(login1.__name__)
print(login1.__doc__)
```



## 5. 匿名函数
+ 基于Lambda定义的函数格式为：Lambda 参数：函数体

其中：

```python
参数：支持任意参数
函数体：只能够支持单行的代码
返回值：默认将函数体单行代码执行的结果返回给出来
```

```python
f1 = lambda x: x + x

res = f1(10)
print(res)
----------------------
def f1(x):
    return x+x
```

```python
# f1 = lambda x: lambda y: x + y

def f1(x):
    def f2(y):
        return x+y
    return f2

res = f1(10)
v1 = res(20)
print(v1)
```



### 5.1 三元运算
```plain
结果 = 条件成立  if   条件   else   条件不成立
```

```python
num = input("请输入一个数字:")

res = 10 if int(num) > 5 else 20
print(res)
```

```python
num = input("请输入一个数字:")

res = lambda x: x + x if int(num) > 5 else 20
v1 = res(20)
print(v1)

num = input("请输入一个数字:")

res = 10 if int(num) > 5 else lambda x: x + x
print(res)
```

## 6. 生成器
### 6.1 迭代器
```python
data_list = [111, 222, 333, 444, 555]
data_iter = iter(data_list)
print(next(data_iter))
print(next(data_iter))
print(next(data_iter))
print(next(data_iter))
```

### 6.2 生成器
生成器就是一种特殊的迭代器，只不过是通过函数构造出来的

```python
def func():
    print(1)
    yield 2
    print(3)
    yield 4
    print(5)
    yield 6
如果在一个函数内出现了yield关键字，那么说明该函数不再是普通的函数，而是一个生成器的函数
此时执行该函数并不会运行其内部的代码，而是会返回一个生成器的对象
此时如果想要执行函数内部的代码，需要使用next方法
生成器会记录程序当前运行的状态，遇见yield即跳出执行，下次再执行时会从上次执行的位置接着往下执行

def func():
    print(1)
    yield 2
    print(3)
    yield 4
    print(5)
    yield 6


res = func()
# print(next(res))
# print(next(res))
# print(next(res))
# print(next(res))
for i in res:
    print(i)
```



### 6.3 生成器的应用场景
#### 6.3.1 做一个简单的秒表
```python
import time


def func():
    s = 0
    while True:
        yield s
        time.sleep(1)
        s += 1


g = func()
while True:
    print(next(g))
```

#### 6.3.2 逐一生成数据
```python
def func(num):
    s = 0
    while s < num:
        res = random.randint(100, 999)  # 从100-999的范围随机挑选一个整数
        yield res
        s += 1


g = func(1000000)
print(next(g))
print(next(g))
print(next(g))
print(next(g))
```

#### 6.3.3 流量控制
```python
import time

traffic = [1231, 2345, 3521, 4142, 5144, 6456]
traffic_iter = iter(traffic)


def traffic_monitor(interval):
    traffic_first = next(traffic_iter)
    time_first = time.time()
    time.sleep(interval)
    while True:
        traffic_last = next(traffic_iter)
        time_last = time.time()
        res = (traffic_last-traffic_first)/(time_last-time_first)
        yield res
        time_first = time_last
        traffic_first = traffic_last
        time.sleep(interval)

g = traffic_monitor(3)
print(next(g))
print(next(g))
```

```python
import time

traffic = [1231, 2345, 3521, 4142, 5144, 6456]
traffic_iter = iter(traffic)


def traffic_monitor(interval):
    traffic_first = next(traffic_iter)
    time_first = time.time()
    time.sleep(interval)
    while True:
        traffic_last = next(traffic_iter)
        time_last = time.time()
        res = (traffic_last - traffic_first) / (time_last - time_first)
        yield res
        time_first = time_last
        traffic_first = traffic_last


def traffic_control(interval):
    g = traffic_monitor(interval)
    while True:
        res = next(g)
        time_first = time.time()
        yield res
        time_last = time.time()
        if (time_last - time_first) < interval:
            time.sleep(interval - (time_last - time_first))


g = traffic_control(3)
print(next(g))  # 0s
time.sleep(2)
print(next(g))  # 2s
```

```python
import time

traffic = [1231, 2345, 3521, 4142, 5144, 6456]
traffic_iter = iter(traffic)
#
#
def traffic_monitor(interval):
    traffic_first = next(traffic_iter) #1231
    time_first = time.time()  #0s
    time.sleep(interval)   #3s
    while True:
        traffic_last = next(traffic_iter) #3521
        time_last = time.time() #6s
        res = (traffic_last - traffic_first) / (time_last - time_first)#(2345-1231)/3  #(3521-2345)/(6-3)
        yield res
        time_first = time_last #3s
        traffic_first = traffic_last #2345

def traffic_control(interval):
    g = traffic_monitor(interval)
    while True:
        res = next(g)
        time_first = time.time() # 3s  #6s
        yield res
        time_last = time.time()  # 4s
        if (time_last - time_first) < interval:
            time.sleep(interval - (time_last - time_first)) #-2s
g = traffic_control(3) # 生成一个生成器对象
print(next(g))  # 0s  #执行生成器函数内部代码  遇到yield暂停返回
time.sleep(1)
print(next(g))  # 4s
# print(next(g))
# print(next(g))
```

### 6.4 send
```python
def func():
    print(1)
    v1 = yield 2
    print(v1)
    v2 = yield 3
    print(v2)


g = func()
print(g.send(None))
g.send(1)
g.send(2)
```



## 7. 推导式
+ 推导式是python中提供了一个非常方便的功能，可以让我们通过一行代码实现创建数据类型的同时初始化一些值

```python
# data_list = []
# for i in range(1,11):
#     data_list.append(i)
# print(data_list)

data_list = [i for i in range(1, 11)]
print(data_list)
```

```python
data_dict = {"ip": f"192.168.1.{i}" for i in range(1, 11)}
print(data_dict)

data_dict = {"ip": {f"192.168.1.{i}" for i in range(1, 11)}}
print(data_dict)

data_dict = {f"ip{i}": f"192.168.1.{i}" for i in range(1, 11)}
print(data_dict)
```

```python
data_dict = (f"192.168.1.{i}" for i in range(1, 11))
# print(next(data_dict))
# print(next(data_dict))
# print(next(data_dict))
for i in data_dict:
    print(i)
```

+ 例子1

```python
def num():
    return [lambda x: i * x for i in range(4)]
result = [m(2) for m in num()]
print(result)
```

```python
def num():
    data_list = []
    for i in range(4):
        def func(x):
            return i * x

        data_list.append(func)
    return data_list


result = []
for m in num():
    result.append(m(2))
print(result)
```



+ 例子2

```python
def num():
    return （lambda x: i * x for i in range(4)）
result = [m(2) for m in num()]
print(result)
```

```python
def num():
    def inner(x):
        for i in range(4):
            yield i * x

    return inner


result = []

for i in num()(2):
    result.append(i)
print(result)
```



## 8. 模块
为了避免一个文件过于庞大，我们往往需要根据功能来进行拆分，如果说将其中的一部分功能封装在其他的一个py文件当中，那么此时这些py文件就叫做模块。如果说功能太多，那么我们也可以创建一个文件夹，用多个py文件来封装这些功能，此时的这个文件夹我们可以把它叫做包。

一般在引入模块时有两种方式

+ import xxx
+ from xxx import xxx

```python
# import test
# test.func()
# test.inner()
from test import func,inner
inner()
```

### 8.1 引入模块的路径问题
+ sys.path可以查看到python默认查找相关模块的路径

```python
import sys
print(sys.path)
```

+ 除了这些默认查找的路径外，会最优先在当前执行文件的目录下查找，因此要避免在当前目录下出现名称与内置模块相同的py文件

### 8.2 引入模块时的其他问题
+ 模块重名可以通过as取别名来进行区分
+ 主文件

```python
在执行py文件时，当前执行文件（模块)  __name__ = "__main__"
如果是导入的其他模块，则__name__等于对应的模块名
```

+ 在项目实际开发过程中，一般在主文件会写上main标记，但不是绝对的，因为其他文件在开发调试的时候也可能会有main

### 8.3 第三方模块
###### <font style="color:#DF2A3F;">常见模块paramiko ncclient pysnmp</font>
Python之所以流行，其中一个非常重要的原因就是支持非常丰富的第三方库，这里说的第三方库指的就是第三方模块。使用第三方模块说的通俗一点就是将别人写好的开源的代码拿来直接使用。

而第三方模块必须要先安装才能使用，常见的安装方式有以下几种：

+ pip

```python
pip3 install 模块名
```

+ 源码安装
+ wheel格式

### 8.4 常见内置模块
+ random

```python
1.random.randint #在一个范围内随机取一个整数
res = random.randint(1,10)
print(res)

2.random.uniform #在一个范围内随机取一个小数
res = random.uniform(1,10)
print(res)

3.random.choice #从一个容器里随机取一个值
l1 = [123, 456, 789, 10]
res = random.choice(l1)
print(res)

4.random.sample #从一个容器里随机取多个值
l1 = [123, 456, 789, 10]
res = random.sample(l1,2)
print(res)

5.random.shuffle # 打乱顺序
l1 = [123, 456, 789, 10]
random.shuffle(l1)
print(l1)
```

+ hashlib

hash算法特征：单向不可逆，随机输入固定输出，雪崩效应

> [http://cmd5.com/](http://cmd5.com/)
>

```python
import hashlib

hash_obj = hashlib.md5("按时发生".encode("utf-8"))
hash_obj.update("admin".encode("utf-8"))
res = hash_obj.hexdigest()
print(res)
```

+ time

```python
import time

1.time.time()

2.time.timezone

3.time.sleep()
```

+ datetime

```python
from datetime import datetime, timezone, timedelta

1.datetime.now() # 记录当前的时间，得到的值是一个时间的类型（考试时会用到）
t1 = datetime.now()
print(t1,type(t1))

2.timezone()  # 获取当前的时区，参数中要传入一个timedelta的类型
t1 = timezone(timedelta(hours=8))
print(t1)

3.datetime.utcnow() #获取utc时间 得到的值也是一个时间的类型
t1 = datetime.utcnow()
print(t1,type(t1))
    
4.时间的相加
t1 = datetime.now()
t2 = timedelta(days=1,hours=2)
print(t1+t2)

5.时间的相减
t1 = datetime.now()
t2 = timedelta(hours=8)
print(datetime.utcnow())
print(t1-t2)

t1 = datetime.now()
t2 = datetime.utcnow()
print(t1-t2)
6.str类型与datetime类型转换
# 1.将datetime类型转成str类型（考试时会用到）strftime()
t1 = datetime.now()
content = t1.strftime("%Y-%m-%d-%H-%M-%S")
print(content,type(content))
with open(f"{content}-CE1.zip","w",encoding="utf-8") as f:
    pass
# 2.将str类型转换成datetime类型
t1 = datetime.now()
content = t1.strftime("%Y-%m-%d-%H-%M-%S")
t2 = datetime.strptime(content, "%Y-%m-%d-%H-%M-%S")
print(t2, type(t2))
```



## 9. JSON格式数据
+ json格式数据主要用在哪里

json格式本质上就是一个字符串，常用于网络传输

```plain
跨语言传输，例如：
A系统用N语言开发，有自己的数据类型
B系统用M语言开发，也有自己的数据类型
由于语言的不同，那么基础数据格式也都不同
为了方便数据传输，大家约定一个格式：json格式，每种语言都是将自己的数据类型转换成json格式，也可以将json格式的数据转换为自己的数据类型
```

+ python中的json模块

```python
l1 = [{"sysname": "CE1", "port": 22, "username": "python"},
      {"sysname": "CE2", "port": 22, "username": "python"},
      {"sysname": "CE3", "port": 22, "username": "python"}]
v1 = json.dumps(l1)
print(v1,type(v1))
```

```python
l1 = """[{"sysname": "CE1", "port": 22, "username": "python"},
      {"sysname": "CE2", "port": 22, "username": "python"},
      {"sysname": "CE3", "port": 22, "username": "python"}]"""
v1 = json.loads(l1)
print(v1, type(v1))
```

    - 数据类型->json，一般称为：序列化 dumps
    - json->数据类型，一般称为：反序列化 loads





## 10. 正则表达式
### 10.1 字符相关
> [https://c.runoob.com/front-end/854/](https://c.runoob.com/front-end/854/)
>

+ "xxxx" 匹配固定内容xxxx



+ [abc] 匹配a或b或c字符 <font style="color:#DF2A3F;"> \[abc]转义成abc 而不是a/b/c</font>



+ [^abc]匹配除了a,b,c之外的其他字符



+ [a-z]匹配a-z的任意字符



+ . 代指除换行符以外的任意字符



+ \w 代指字母或数字或下划线（汉字）



+ \d 代指数字



+ \s代指任意的空白符，包括空格、制表符等等



### 10.2 数量相关
+ `*` 重复0次或更多次



+ `+`  重复1次或更多次



+ `?`  重复0次或1次

贪婪匹配与非贪婪匹配	

+ 贪婪匹配：在满足正则表达式的情况下匹配的越长越优先
+ 非贪婪匹配：在满足正则表达式的情况下匹配的越短越优先



+ `{n}`  重复n次



+ `{n,}`  重复n次或更多次



+ `{n,m}`  重复n到m次



### 10.3 括号（分组）
+ `（）`  并不会影响到匹配的结果，而是在匹配到之后，只提取出括号当中的值，如果加了多个括号，则将多个括号的内容都提取出来，然后将每个括号中匹配到的结果当做一个元组中的元素
+ ?<= 前置肯定符
+ ?= 后置肯定符
+ ?: 取消分组优先



### 10.4 起始和结束
用于验证必须是指定的内容开头和结尾

+ `^`  起始



+ `$`  结束

```plain
^#\n.+\n$
```

### 10.5 转义
> 由于正则表达式中* . \ { } ( )等都具有特殊的含义，所以如果想要在正则中匹配这种指定的字符，就需要进行转义
>

+ 正则匹配ip地址

```plain
0-254 匹配正则时 按照一个字符为单位来进行匹配
0-254这个范围内其实包含了三种类型的数字  分别是个位数  十位数 百位数
因此最初步的划分  将0-254分为三个范围
[0-9]
[10-99]
[100-254]
但是这样划分在表示百位数的时候遇到一些问题
如果这样写[1-2][0-5][0-4]   199是匹配不到的   249也是匹配不到的
因此需要对百位数进行进一步的划分
100-199
200-249
250-254

因此 最终经过划分得到的是五段
0-9  
10-99
100-199
200-249
250-254
表示成正则表达式
\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4]
这样写完之后，还是按照单个字符来匹配的 ，因为对于ip地址而言\d永远能够满足匹配规则
因此需要将\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4]当做一个整体来进行匹配，并且有一个末尾
的标识，在匹配Ip地址时，可以采用.来作为末尾的标识

(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.

由于ip地址是x.x.x.x的形式
因此可以将x.作为一个整体匹配三次 匹配后的结果为x.x.x.
((\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}
最后一个x的匹配规则跟前面相同，但是也需要做一个结尾的标识，此时可以使用非数字来作为结尾的标识
((\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])[^\d]
此时已经可以正常的匹配到ip地址了
那么就只剩下一个问题，就是最终显示的结果不想包含结尾的标识，那么可以使用后置肯定符隐藏该标识符
所以最终的正则表达式写成
((\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d])
```

+ 通过Ip来匹配接口

```plain
通过ip来匹配接口
在交换机当中，每段配置之间都有一个#的连接符，那就可以以该符号作为标识符
最开始可以将相关内容全部匹配到后续再完成优化
例如：
#
interface Vbdif20002
ip binding vpn-instance HCIE(56369-VPCA-20005
ip address 192.168.10.1 255.255.255.0
arp distribute-gateway enable
mac-address 0000-5e00-0102
arp collect host enable
由于是使用ip来匹配接口，那么就需要找到ip与接口的对应关系
因此可以将下面这段先匹配出来
#
interface Vbdif20002
ip binding vpn-instance HCIE(56369-VPCA-20005
ip address 192.168.10.1
此时的正则可以写成是#\n.+\n.+\n.+192.168.10.1
后续的优化包括：隐藏不需要的内容以及调整正则的长度
如果想要隐藏不需要的内容，就要找到标识符
对于前置的标识符  可以用接口前的空格来表示  对于后置的肯定符 可以用接口后的换行符来表示
此时正则可以写成是(?<=#\n.+\s).+(?=\n.+\n.+192.168.10.1)
后置肯定符当中的\n.+还可以进行优化  最终正则写成是(?<=#\n.+\s).+(?=(\n.+){2}192.168.10.1)
```



### 10.6 re模块常见方法
+ findall，获取匹配到的所有数据，以列表的形式进行返回

```python
import re

with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    res = re.findall(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))',
                     string=content)
    print(res)
#?:取消括号优先
```

+ match，从起始位置开始匹配，匹配成功返回一个对象，未匹配成功返回None

```python
import re

with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    res = re.match(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))',
                     string=content)
    v1 = res.group()
    v2 = res.groups()
```

之后使用<font style="color:#DF2A3F;">对象</font>.group()或者对象.groups()的方式可以返回匹配的结果

group()返回的是一个字符串，而使用groups()返回的是一个元组

+ search，浏览整个字符串去匹配第一个，未匹配成功返回None

```python
with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    res = re.search(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))',
                     string=content)
    v1 = res.group()
    print(v1)
    v2 = res.groups()
    print(v2)
```

之后使用对象.group()或者对象.groups()的方式可以返回匹配的结果

group()返回的是一个字符串，而使用groups()返回的是一个元组

+ sub，替换匹配成功的位置,返回替换后的字符串

```python
with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    res = re.sub(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))',
                     string=content,count=2,repl="哈哈哈")
    print(res)
```

pattern：指的就是正则表达式

string：指的是匹配的文本

count：指的是替换多少次

repl：指的是将正则匹配到的文本替换成什么内容

+ split，根据匹配成功的位置分割

```python
with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    res = re.split(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))',
                     string=content,maxsplit=3)
    print(res)
```

maxsplit: 最大分割次数

+ finditer，根据匹配的内容生成一个迭代器

```python
with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    res = re.finditer(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))',
                     string=content)
    # print(next(res).group())
    # print(next(res).group())
    # print(next(res).group())
    # print(next(res).group())
    for i in res:
        print(i.group())
```

+ compile，创建一个正则表达式的对象

```python
with open("leaf.txt", "r", encoding="utf-8") as f:
    content = f.read()
    re_obj = re.compile(pattern='((?:(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])\.){3}(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-4])(?=[^\d]))')
    res = re_obj.findall(content)
    v1 = re_obj.search(content)
    print(res)
    print(v1.group())
```

```python
content = "我爱大保健"
res = re.findall(pattern="(?<=我)((?:爱)(?:大)(?:保))(?=健)",string=content)
print(res)
```



## 11. 函数编程项目<font style="color:#DF2A3F;">开发规范</font>
### 11.1 单文件应用
当基于python开发简单应用时（一个python文件可以搞定），需要注意以下几点：

1. 脚本注释
2. 导入模块时，按照内置，第三方，自定义的顺序
3. 全局变量要大写
4. 函数名命名规范以及函数功能注释
5. TODO信息
6. 部分功能代码注释
7. 主文件



### 11.2 单可执行文件
新创建一个项目，假设名字叫【test】，可以创建如下文件和文件夹来存放代码和数据

```plain
test
├── app.py        文件，程序的主文件（尽量精简）
├── config.py     文件，配置文件（放相关配置信息，代码中读取配置信息，如果想要修改配置，即可以在此修改，不用再去代码中逐一修改了）
├── db            文件夹，存放数据
├── files         文件夹，存放文件
├── src           包，业务处理的代码
└── utils         包，公共功能
```

+ app.py ： 文件，程序的主文件(尽量精简)
+ config.py : 文件，配置文件（存放相关配置信息，代码中读取配置信息，如果想要修改配置，即可以在此修改，不用再去代码中逐一修改了）
+ db ： 文件夹，存放数据
+ files ： 文件夹，存放文件
+ src ： 包，业务处理的代码
+ utils ： 包，存放公共功能



### 11.3 多可执行文件
```plain
test_new
├── bin					文件夹，存放多个主文件（可运行）
│   ├── app1.py
│   └── app2.py
├── config              包，配置文件
│   ├── __init__.py
│   └── settings.py
├── db                  文件夹，存放数据
├── files               文件夹，存放文件
├── src                 包，业务代码
│   └── __init__.py
└── utils               包，公共功能
    └── __init__.py
```
