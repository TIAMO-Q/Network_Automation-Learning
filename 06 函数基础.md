### 1. 初始函数
#### 1.1 什么是函数
```plain
函数可以看成是一大堆代码组成的功能的集合，比如说小时候大家都玩过积木
现在假设我们可以用积木来组装成一个汽车人。如果按照我们之前编写代码的方式
那每一次需要汽车人的时候，都要用最基本的积木零件开始组装。那现在我们可以采用另外一种方式
就是将组合好的汽车人保存起来，之后每一次要使用的时候就不用再重复繁琐的过程
这样的方式就是将代码打包成函数
```

#### 1.2 函数的定义
```python
def 函数名():
```

#### 1.3 函数的调用
```python
def func():
    print(123)
    print(456)


func()
```

#### 1.4 函数示例
```python
def func():
    print(123)
    print(456)


func()
```

#### 1.5 函数的应用场景
+ 提高代码的利用率

```python
# 一个检测网络设备状态的功能

if cpu_usage > 90%
    #发送告警邮件
    ...
    
if ospf_state != "full"
    #发送告警邮件
    ...
    
if power = "Abnormal"
    #发送告警邮件
    ...
```

假设发送告警邮件的代码有10行，那么每监控一个状态都要重复的写这10行告警邮件的代码，而此时如果我们将这十行代码打包成一个功能函数，这样我们每次需要这个功能只需要调用函数即可

```python
def email_send():
    #发送告警邮件
    
if cpu_usage > 90%
    email_send()
    
if ospf_state != "full"
    email_send()
    
if power = "Abnormal"
    email_send()
```

## 2. 函数的参数
+ 发送邮件模板

```python
import smtplib
from email.mime.text import MIMEText
from email.utils import formataddr

# 1.邮件内容配置 
msg = MIMEText(邮件内容, 'html', 'utf-8') 
msg['From'] = formataddr([邮件发送人, 发送邮箱])
msg['Subject'] = 邮件主题

# 2.发送邮件  
server = smtplib.SMTP_SSL("smtp.126.com")
server.login(邮箱地址, 授权码)
server.sendmail(发送邮箱, 接收邮箱, msg.as_string())
server.quit()
```



#### 2.1 形参和实参
```plain
通过上面的案例可以看出来，我们在创建函数时，有时会遇到函数里面的某些值需要变化的情况
这时候就需要定义类似于变量的东西，根据需求传入不同的参数，从而获取不同的值
而在定义函数时加入的参数就叫做形参，在调用函数时传入的参数就叫做实参
实参是人为根据情况来改变的，而形参就可以接收实参的值赋值给函数中的变量
```

```python
def send_mail(mail): # 形参
    # 1.邮件内容配置
    msg = MIMEText("今夜老婆不在家，偷偷学习py", 'html', 'utf-8')
    msg['From'] = formataddr(["一个帅哥", "kouest123@126.com"])
    msg['Subject'] = "好好学习"

    # 2.发送邮件
    server = smtplib.SMTP_SSL("smtp.126.com")
    server.login("kouest123@126.com", "FDLUQFTLXTMLLATG")
    server.sendmail("kouest123@126.com", mail, msg.as_string())
    server.quit()

send_mail("3144836997@qq.com")
send_mail("307120469@qq.com")
send_mail("kouest123@126.com")
```

```python
import telnetlib
import time
tel = telnetlib.Telnet(host="192.168.100.1",port=23)
tel.read_until(b"Password:")
tel.write("Huawei@123\n".encode("utf-8"))
time.sleep(2)
tel.write("screen-length 0 temporary\n".encode("utf-8"))
time.sleep(2)
tel.write("display current-configuration\n".encode("utf-8"))
time.sleep(2)
print(tel.read_very_eager().decode("utf-8"))
```

```python
import telnetlib
import time

def func(ip):
    tel = telnetlib.Telnet(host=ip,port=23)
    tel.read_until(b"Password:")
    tel.write("Huawei@123\n".encode("utf-8"))
    time.sleep(2)
    tel.write("screen-length 0 temporary\n".encode("utf-8"))
    time.sleep(2)
    tel.write("display current-configuration\n".encode("utf-8"))
    time.sleep(2)
    print(tel.read_very_eager().decode("utf-8"))

func("192.168.100.1")
func("192.168.100.2")
func("192.168.100.3")
```

#### 2.2 位置参数和关键字参数
```python
def func(name, age):
    print(name)
    print(age)


func(24, "zhangsan")
```

```python
def func(name, age):
    print(name)
    print(age)


func(age=24,name="张三")
```

无论是位置参数还是关键字参数，一定要保证形参和实参的数量是一致的

#### 2.3 默认参数
```python
def func(name, age, gender="male"):
    print(gender)
    print(name)
    print(age)


func(age=24, name="张三", gender="female")
```

#### 2.4 动态参数（可变长参数）
```python
def func(name, age, gender="male"):
    print(gender)
    print(name)
    print(age)


func(24,"张三","123","456")
```

+ 可变长的位置参数

```python
def func(*args):
    print(args)


func("123","456","789","asfas","sgsrg")
```

+ 可变长的关键字参数

```python
def func(**kwargs):
    print(kwargs)


func(name="张三", age=18, gender="male")
```

```python
def func(*args,**kwargs):# 可以接收任意数量的位置参数和任意数量的关键字参数
    print(args)
    print(kwargs)


func("张三", age=18, gender="male")
```

+ 在传参时一定要注意，位置参数在关键字参数的前面

```python
def func(*args, **kwargs):
    print(args)
    print(kwargs)


func("张三", age=18, gender="male","李四")#李四位置错误
```



## 3.函数的返回值
#### 3.1 返回值的基本概念
```python
def func(weight, height):
    bmi = weight / height ** 2
    print(bmi)
    return bmi


res = func(weight=75, height=1.8)
print(res)
```

#### 3.2 返回值的特点
+ 返回值可以是任意类型，如果函数中没写return或直接 return，则默认返回None。

```python
def func(weight, height):
    bmi = weight / height ** 2
    # print(bmi)
    return


res = func(weight=75, height=1.8)
print(res)
```

+ return后面的值如果有逗号，则默认会将返回值转换成<font style="color:#DF2A3F;">元组</font>再返回。

```python
def func(weight, height):
    bmi = weight / height ** 2
    return 1, 2, 3


res = func(weight=75, height=1.8)
print(res)
```

+ 函数一旦遇到return就会立即退出函数（终止函数中的所有代码）

```python
def func(weight, height):
    bmi = weight / height ** 2
    return 1, 2, 3
    print(123)
    print(456)


res = func(weight=75, height=1.8)
print(res)
```

直接灰掉了

<img src="https://cdn.nlark.com/yuque/0/2026/png/50508853/1778659275231-5c864944-4c6c-40ee-bac4-9c3682c83a1e.png" width="307.3333333333333" title="" crop="0,0,1,1" id="u7ff15176" class="ne-image">

#### 3.3 返回值的应用场景
+ 执行完某个功能后返回我们想要的结果
    - 执行完登录函数返回登录成功还是失败
    - 执行发送邮件函数返回发送成功还是失败
    - 执行完登录函数返回一个客户端对象
    - 做完数据处理函数之后返回处理后的结果
+ 用于终止函数的执行

## 4. 内存地址
#### 4.1 如何查看内存地址
```python
def func():
    pass


print(id(func))
```

#### 4.2 函数传递内存地址
+ 函数在进行参数传递时，传递的是<font style="color:#DF2A3F;">内存地址</font>

```python
def func(a1):
    print(id(a1))


v1 = 123
print(id(v1))
func(v1)
```

+ 传递内存地址的好处

```python
def func(a1):
    print(id(a1))
    a1.append(666)


v1 = [123,456,789]
print(id(v1))
func(v1)
print(v1)
```

    - 可以利用函数对可变长数据类型进行更改、
    - 可以节约内存
+ 深拷贝

```python
import copy

v1 = [123, 456, 789]
v2 = copy.deepcopy(v1)
print(id(v1))
print(id(v2))
print(v1, v2)
v2.append(666)
print(v1)
```

```python
import copy

v1 = [123, 456, 789]
v2 = copy.deepcopy(v1)

func(v2)
print(v1)
```



#### 4.3 深浅拷贝详解
1. 深浅拷贝一般说的都是可变类型，例如列表，集合，字典。因为不可变类型在进行深浅拷贝时无意义，内部都不会去拷贝，而是让拷贝前后的数据指向同一块内存地址

```python
import copy

v1 = 123
v2 = copy.deepcopy(v1)
print(id(v1),id(v2))
```

2. 可变的数据类型在浅拷贝时，拷贝的只是数据类型本身，而对于内部的数据依然采用指向同一块内存地址的方式

```python
import copy

v1 = [123, 456, 789]
v2 = copy.deepcopy(v1)
print(id(v1[0]), id(v2[0]))
print(id(v1[1]), id(v2[1]))
```

3. 对于深拷贝而言，针对可变类型，无论在哪一层都会拷贝一份

```python
import copy

v1 = [123, 456, 789, [123, 456]]
v2 = copy.deepcopy(v1)
print(id(v1), id(v2))
print(id(v1[0]), id(v2[0]))
print(id(v1[1]), id(v2[1]))
print(id(v1[3]), id(v2[3]))
```

4. 虽然元组是不可变类型，但是元组中如果有可变的数据类型，也会被拷贝

```python
import copy

t1 = (1, 2, 3, [1, 2, 3])
t2 = copy.deepcopy(t1)
print(id(t1), id(t2))
```

#### 4.4 函数返回内存地址
```python
def func():
    v1 = 123
    print(id(v1))
    return v1


res = func()
print(id(res))
```

1. 定义一个名为func的函数
2. 执行func函数
3. 在函数内部定义了变量v1=123   此时相当于v1指向了一块内存地址，假设为A，此时A的引用计数器+1
4. 输出了A的内存地址
5. 返回A的内存地址
6. res接收A的内存地址，此时res指向A，引用计数器为2
7. 函数执行完成之后，会回收函数体内的资源，引用计数器-1

#### 4.5 内存的驻留机制
python3.6需要考虑的问题

对于一些常用的值，在python当中为其预留了内存地址，可以起到节约内存地址的作用

#### 4.6 默认参数的内存地址
1. 函数在定义时，如果定义了默认参数，则会直接为该默认参数生成一个内存的地址，a2指向该内存地址
2. 在函数调用时，如果传递了新的参数，则此时a1和a2会指向新传入的参数对应的内存地址
3. 只要传递了实参，会指向一块新的内存地址
+ 当默认参数为可变长类型时

```python
def func(a1, a2=[1, 2]):
    a2.append(a1)
    print(a1)
    print(a2)


func(1)
func(2)
func(3, [123])
func(4)
func(5, [123])
```

## 5. 解包
```python
def func(*args, **kwargs):
    print(args)
    print(kwargs)


switch1 = ["CE1", 22, "python", "Huawei@123"]
switch2 = {"sysname": "CE2", "port": "22", "username": "python", "password": "Huawei@123"}
func(*switch1, **switch2) 
等价于
func("CE1", 22, "python", "Huawei@123", sysname="CE2", port=22, username="python", password="Huawei@123")
```

## 6. 函数名
函数名本质上也是变量名

```python
def func():
    print(123)
    return "hahah"


func_list = [123, "456", func, func()]
# print(func_list[2])
# print(func_list[3])

func_list[2]()
```

+ 函数本身是可以被哈希的，因此对于一些必须可哈希的数据类型，也可以将函数作为其中的元素

```python
def func():
    print(123)
    return "hahah"


s1 = {func, func, func, func}

for i in s1:
    print(i)
```

```python
def cpu_state():
    return "cpu_state normal"


def power_state():
    return "power fault"


def fan_state():
    print(123)


message = """
1.cpu_state
2.power_state
3.fan_state
"""
print(message)
device_dict = {"1": cpu_state, "2": power_state, "3":fan_state}
user_input = input("请输入序号查询：")
print(device_dict[user_input]())
```

+ 由于函数名的这个本质，因此在编写程序时，需要注意以下几点



    - 定义多个相同的函数名，后定义的会覆盖之前定义的
    - 不要定义与python内部函数相同名称的函数
+ 函数也可以被当成是参数传递和返回

```python
def f1():
    print(123)


def f2(func):
    func()


f2(f1)
```

```python
def f1():
    print(123)


def f2():
    return f1


res = f2()
res()
```





## 7. print 和return的区别
+ print是在某个位置来输出一段内容
+ return是在函数执行之后有一个返回的结果，便于后续再进行赋值和计算

```python
def func(a1, a2):
    print(a1 + a2)
    return a1 + a2


res = func(10, 20)
print(res)
```
